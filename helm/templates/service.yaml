apiVersion: v1
kind: Service
metadata:
  name: {{ include "kwakeup.fullname" . }}
  labels:
    {{- include "kwakeup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kwakeup.selectorLabels" . | nindent 4 }}
