{{- if and .Values.postgres.enabled (not .Values.postgres.existingSecret) }}
{{- $secretName := printf "%s-postgres" (include "kwakeup.fullname" .) }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- if not $existing }}
{{- /*
  Rendered only on first install (when the secret does not yet exist).
  On upgrade, the lookup finds the existing secret and this block is skipped
  entirely — Helm never touches the secret, so passwords cannot be silently
  rotated. The 'keep' policy set on creation prevents Helm from pruning the
  secret as an orphan resource on uninstall or upgrade.
*/}}
{{- $adminPassword := .Values.postgres.postgresPassword | default (randAlphaNum 24) }}
{{- $appPassword := .Values.postgres.appPassword | default (randAlphaNum 24) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "kwakeup.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
stringData:
  postgres-user: {{ .Values.postgres.postgresUser | quote }}
  postgres-password: {{ $adminPassword | quote }}
  postgres-db: {{ .Values.postgres.postgresDatabase | quote }}
  app-user: {{ .Values.postgres.appUser | quote }}
  app-password: {{ $appPassword | quote }}
  dsn: {{ printf "host=%s port=%d user=%s password=%s dbname=%s sslmode=disable"
    (printf "%s-postgres" (include "kwakeup.fullname" .))
    (int .Values.postgres.service.port)
    .Values.postgres.appUser
    $appPassword
    .Values.postgres.postgresDatabase | quote }}
{{- end }}
{{- end }}
